import React from 'react';

export default function ProtectedRoute({ children, pageName }) {
  // Basic route protection - in v1.0-STABLE this is minimal
  // Advanced protection logic can be added in future versions
  
  return children;
}